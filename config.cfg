# ─── Sylph Pipeline Configuration ────────────────────────────────────────────
# Copy this file, fill in your paths, then run:
#   bash pipeline.sh --input test.csv --config config.cfg
#
# All paths can be absolute or relative to the directory where you run pipeline.sh
# Lines starting with # are comments and are ignored.

# ─── Database ─────────────────────────────────────────────────────────────────
# Path to the sylph database file (.syldb)
DATABASE=./db_100c_all_genomes_updated.syldb

# ─── Taxonomy metadata files ──────────────────────────────────────────────────
# Required by sylph-tax taxprof (space-separated if multiple, or one per line)
METADATA_UHGG=./metadata/uhgg2_metadata.tsv
METADATA_PHAGE=./metadata/all_phage_genomes_taxo.tsv

# ─── Output & working directories (optional — defaults shown) ─────────────────
# RESULTS_DIR=./results
# DATA_DIR=./data
# LOGS_DIR=./logs

# ─── Runtime options (optional — defaults shown) ──────────────────────────────
# MAX_JOBS=8
